/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true,  // Asegúrate de que el experimental appDir esté habilitado
    },
  }
  
  module.exports = nextConfig;
  